// import Person from './person';

function CityView(city) {
  this.id = city._id;
  this.name = city.name;
  this.balance = city.balance;
  // this.country = person.cities;
}

module.exports = CityView;
